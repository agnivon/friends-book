import { ConfirmPasswordMatchDirective } from './confirm-password-match.directive';

describe('ConfirmPasswordMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmPasswordMatchDirective();
    expect(directive).toBeTruthy();
  });
});
