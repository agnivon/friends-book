import { VisiblePostsPipe } from './visibleposts.pipe';

describe('VisiblePostsPipe', () => {
  it('create an instance', () => {
    const pipe = new VisiblePostsPipe();
    expect(pipe).toBeTruthy();
  });
});
